# OSL — Odor Source Localization RL
# Tested with Python 3.10 (conda env `osl`).
#
# torch is pinned to the CPU build used here; for a CUDA build install torch
# separately from https://pytorch.org and remove the line below.

# --- core (training + eval: src/ + train.py/eval.py/main.py) ---
torch==2.12.0
numpy==2.4.4
gymnasium==1.3.0
matplotlib==3.10.9
imageio==2.37.3
tensorboard==2.20.0          # optional logging; ppo_agent skips it if missing

# --- analysis pipeline (analysis/osl2d) ---
scipy==1.17.1                # phase1 cast-PSD (Welch)
scikit-learn==1.8.0          # linear probes + PCA
umap-learn==0.5.12           # phase2a latent visualization
